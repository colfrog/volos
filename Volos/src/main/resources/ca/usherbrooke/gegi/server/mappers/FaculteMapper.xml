<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.usherbrooke.gegi.server.mappers.FaculteMapper">

    <select id="selectFaculte" resultType="String">
        SELECT
            *
        FROM
            faculte;
    </select>

    <select id="insertFaculte">
        INSERT INTO departement
            nom_departement,
            nom_faculte
        VALUES (#{departement.nomDepartement},
            #{departement.nomFaculte});
    </select>

</mapper>